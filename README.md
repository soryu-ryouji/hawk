# hawk

对标 Eagle 的开源图片素材管理工具

## 核心特性

### 非侵入式资源管理

hawk 不会将你的素材"导入"到某个专有仓库中。你的文件始终保存在原来的文件夹里，hawk 只是在一旁建立索引。

```text
你的素材目录/
├── 设计素材/
│   ├── 海报/
│   └── 图标/
├── 摄影/
│   ├── 风景/
│   └── 人像/
└── .hawk/              ← hawk 只在目录下创建这个隐藏文件夹
    ├── config.toml     ← 项目级配置
    ├── metadata/       ← 素材参数（纯文本，参与同步）
    ├── thumbnails/     ← 缩略图缓存（本地专用，不参与同步，可重建）
    └── hawk.db         ← SQLite 索引缓存（本地专用，不参与同步，可重建）
```

每个文件都单独计算 hash，方便进行文件的参数管理。

**好处**

- 卸载 hawk 后，你的素材文件纹丝不动
- 原有的文件夹组织习惯完全保留
- 与网盘（Dropbox、iCloud、Syncthing、OneDrive）天然兼容

### 纯文本元数据存储

素材文件和文件夹中不存放任何 hawk 的文件。所有素材参数（标签、评分、备注等）集中存放在 `.hawk/metadata/` 中，按内容哈希命名，每个素材对应一个独立的纯文本文件。

为什么用纯文本？

- 网盘同步安全：每个文件独立，冲突只影响单个素材，不会破坏整个数据库
- 版本控制友好：可以用 Git 管理素材库，diff / merge / rollback 都可用
- 数据透明：随时可用文本编辑器查看和修改，没有数据锁定
- 人类可读：TOML 格式带注释，比 JSON/YAML 更适合手写

同时，hawk 会在 `.hawk/hawk.db` 维护一个 SQLite 索引缓存，用于实现毫秒级搜索。索引缓存和缩略图都是本地专用文件，不参与网盘同步（`.hawk/.gitignore` 自动排除），可以随时删除重建——真正的"数据源"永远是 `metadata/` 下的纯文本文件。

### 项目配置

每个项目的设置保存在自己目录下的 `.hawk/config.toml` 中，随素材目录一起同步、备份，不污染全局环境：

```toml
# .hawk/config.toml 示例

# 项目名（界面显示用）
name = "设计素材库"

# 索引时忽略的路径
ignore = ["node_modules", "*.tmp"]

# 生成的缩略图尺寸
thumbnail_sizes = [256, 1024]
```

全局配置文件位于 `~/.config/hawk/config.toml`，只存放跨项目的全局设置（目前没有全局配置项）。

### 内容寻址（Content-Addressable）

每个配置文件通过其 内容哈希（BLAKE3）进行唯一标识，缩略图也按 hash 存储，避免重复生成：

```text
.hawk/thumbnails/
├── 256/                    # 列表视图
│   ├── ab/
│   │   └── abcdef123...webp
│   └── cd/
└── 1024/                   # 预览面板
    ├── ab/
    └── cd/
```

### 实时文件监听

hawk 通过文件系统事件实时感知变化，新增、删除、重命名、修改文件时，索引自动更新。

### 开放 REST API

hawk 内置 HTTP 服务器，提供完整的 REST API，方便第三方工具接入：

```text
# 搜索素材
GET http://localhost:8080/api/search?q=logo&tags=品牌&rating=5

# 获取缩略图
GET http://localhost:8080/api/thumb/abc123?size=256

# 添加标签
POST http://localhost:8080/api/files/abc123/tags
{ "name": "待审核", "color": "#FF0000" }
```

## 架构设计

```text
┌──────────────────────────────────────────────────────────────┐
│                     用户素材目录 (Workspace)                   │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐   │
│  │ design.jpg  │  │ photo.png   │  │ .hawk/              │   │
│  │ （素材文件和 │  │ 目录中不存放 │  │   ├── config.toml   │   │
│  │  任何 hawk   │  │  hawk 配置   │  │   ├── metadata/     │   │
│  │  配置文件）  │  │  文件）      │  │   ├── thumbnails/   │   │
│  └─────────────┘  └─────────────┘  │   └── hawk.db       │   │
│                                    └─────────────────────┘   │
│         ↑                                                    │
│         │  元数据集中在 .hawk/metadata/（唯一真相，同步安全）   │
└─────────┼────────────────────────────────────────────────────┘
          │
          ▼
┌──────────────────────────────────────────────────────────────┐
│                      .hawk/ 内部                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐   │
│  │ config.toml │  │ metadata/   │  │ thumbnails/ hawk.db │   │
│  │ (项目配置)   │  │ (素材参数)   │  │ (缩略图/索引缓存)    │   │
│  │ 参与同步     │  │ 参与同步     │  │ 本地专用，不参与同步  │   │
│  └─────────────┘  └─────────────┘  │ 可重建               │   │
│                                    └─────────────────────┘   │
└──────────────────────────────────────────────────────────────┘
          │
          ▼
┌──────────────────────────────────────────────────────────────┐
│                ~/.config/hawk/config.toml                     │
│                全局配置（暂无全局配置项）                        │
└──────────────────────────────────────────────────────────────┘
          │
          ▼
┌──────────────────────────────────────────────────────────────┐
│                      hawk Core                               │
│  ┌──────────────┐ ┌──────────────┐ ┌─────────────────────┐   │
│  │ File Watcher │ │ Hash Engine  │ │ Thumbnail Generator │   │
│  │ (chokidar/   │ │ (BLAKE3/     │ │ (Sharp/libvips)     │   │
│  │  native API) │ │  xxHash64)   │ │                     │   │
│  └──────────────┘ └──────────────┘ └─────────────────────┘   │
│  ┌──────────────┐ ┌──────────────┐                           │
│  │ Index Cache  │ │ Search Engine│                           │
│  │ (SQLite)     │ │ (FTS5)       │                           │
│  └──────────────┘ └──────────────┘                           │
└──────────────────────────────────────────────────────────────┘
          │
          ▼
┌──────────────────────────────────────────────────────────────┐
│                     REST API Server                          │
│  GET  /api/workspaces                                        │
│  GET  /api/files                                             │
│  GET  /api/files/:hash                                       │
│  GET  /api/search?q=...                                      │
│  GET  /api/thumb/:hash?size=256|1024                         │
│  POST /api/tags                                              │
│  GET  /api/events (SSE)                                      │
└──────────────────────────────────────────────────────────────┘
          │
          ▼
┌──────────────────────────────────────────────────────────────┐
│                      生态接入                                │
│  浏览器插件 • Figma 插件 • VS Code 扩展 • CLI 工具           │
└──────────────────────────────────────────────────────────────┘
```